- @posts.each do |post|
  %article
    = render('admin_links', post: post) if signed_in?
    %h1= link_to post.title, blog_path(post.slug)
    = markdown(post.body[0..195])
    %p.read_more= link_to 'Read more...', blog_path(post.slug)
