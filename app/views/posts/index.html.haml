- @posts.each do |post|
  %article
    - if signed_in?
      .admin
        = "*un-published" unless post.published
        = link_to 'edit', edit_post_path(post)
        = "-"
        = link_to 'delete', post_path(post), method: :delete, confirm: "You sure?"
    :markdown
      #{post.body}
